/**
 * This code was autogenerated from a Postman source
 */
const http = require('http');
const PORT = process.env.PORT || 4500;

const server = http.createServer((req, res) => {
  const url = new URL(req.url || '', 'http://localhost');
  const pathname = url.pathname;

  // @endpoint GET /users
  if (req.method === 'GET' && pathname === '/users') {
    res.writeHead(200, {"Content-Type":"application/json"});
    return res.end(JSON.stringify({"data":[{"id":1,"name":"Ana Popescu","email":"ana@example.com"},{"id":2,"name":"Mihai Ionescu","email":"mihai@example.com"}],"page":1,"total":2}));
  }

  // @endpoint GET /users/1
  if (req.method === 'GET' && pathname === '/users/1') {
    const requestedStatusCode = req.headers['x-mock-response-code'];

    // "404 Not Found - User missing" — when x-mock-response-code: 404
    if (requestedStatusCode === '404') {
      res.writeHead(404, {"Content-Type":"application/json"});
      return res.end(JSON.stringify({"error":"Not Found","message":"User with id 999 does not exist"}));
    }

    // "200 OK - User found" — default
    res.writeHead(200, {"Content-Type":"application/json"});
    return res.end(JSON.stringify({"id":1,"name":"Ana Popescu","email":"ana@example.com","createdAt":"2026-01-15T09:30:00Z"}));
  }

  // @endpoint POST /users
  if (req.method === 'POST' && pathname === '/users') {
    const requestedStatusCode = req.headers['x-mock-response-code'];

    // "400 Bad Request - Validation error" — when x-mock-response-code: 400
    if (requestedStatusCode === '400') {
      res.writeHead(400, {"Content-Type":"application/json"});
      return res.end(JSON.stringify({"error":"ValidationError","message":"name and email are required"}));
    }

    // "201 Created" — default
    res.writeHead(201, {"Content-Type":"application/json"});
    return res.end(JSON.stringify({"id":3,"name":"New User","email":"new.user@example.com","createdAt":"2026-07-31T10:00:00Z"}));
  }

  // @endpoint PUT /users/1
  if (req.method === 'PUT' && pathname === '/users/1') {
    res.writeHead(200, {"Content-Type":"application/json"});
    return res.end(JSON.stringify({"id":1,"name":"Ana Popescu Updated","email":"ana.updated@example.com","updatedAt":"2026-07-31T10:05:00Z"}));
  }

  // @endpoint PATCH /users/1
  if (req.method === 'PATCH' && pathname === '/users/1') {
    res.writeHead(200, {"Content-Type":"application/json"});
    return res.end(JSON.stringify({"id":1,"name":"Ana Popescu","email":"ana.new-email@example.com","updatedAt":"2026-07-31T10:07:00Z"}));
  }

  // @endpoint DELETE /users/1
  if (req.method === 'DELETE' && pathname === '/users/1') {
    const requestedStatusCode = req.headers['x-mock-response-code'];

    // "404 Not Found - Already deleted" — when x-mock-response-code: 404
    if (requestedStatusCode === '404') {
      res.writeHead(404, {"Content-Type":"application/json"});
      return res.end(JSON.stringify({"error":"Not Found","message":"User with id 999 does not exist"}));
    }

    // "204 No Content - Deleted" — default
    res.writeHead(204, {"Content-Type":"text/plain"});
    return res.end("");
  }

  res.writeHead(404, { 'Content-Type': 'application/json' });
  res.end(JSON.stringify({ error: 'Endpoint not defined' }));
});

server.listen(PORT);
